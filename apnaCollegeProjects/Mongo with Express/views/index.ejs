<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>whatsapp</title>
    <link rel="stylesheet" href="style.css" />
</head>
<body>
    <h1>Whatsapp Chats</h1>
    <form method="GET" action="/chats/new"><button>Create new chat</button></form>
    <% for(let chat of chats){ %>
        <div class="chat">
            <p>From: <i> <%=  chat.from %></i></p>
            <div class="msg">
             <p>   <%=chat.msg %>  </p>
             
            </div><form method="get" action="chats/<%=chat._id%>/edit"><button>Edit</button></form>
            <form method="POST" action="chats/<%=chat.id%>?_method=DELETE">
                <button>Delete</button>
            </form>
            <form method="GET" action="chats/<%=chat.id%>">
                <button>Show</button>
            
            </form>
            <p>Received by: <i> <%= chat.to %></i></p>
            <hr/>
            <p><%= chat.created_at.toString().split(" ")[4]%></p>
            <p><%=chat.created_at.toString().split(" ").slice(0, 4).join(" ") %></p>
        </div>
        <hr/>
    <%}%>
</body>
</html>